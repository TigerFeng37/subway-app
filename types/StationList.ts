import StationType from './Station';

export default interface StationListType {
    [key: string]: StationType
    // closest?: StationType;
    // next?: StationType;
    // default?: StationType;
}